#include "fourier.h"
#include "graphics/opengl.h"

/*fourierhdl::fourierhdl()
{
}

fourierhdl::~fourierhdl()
{

}

void fourierhdl::render(int vertex_location)
{
	vec3f geometry[4];
	int indices[6];

	geometry[0] = vec3f(-radius, -radius, 0);
	geometry[1] = vec3f(radius, -radius, 0);
	geometry[2] = vec3f(radius, radius, 0);
	geometry[3] = vec3f(-radius, radius, 0);
	indices[0] = 0;
	indices[1] = 1;
	indices[2] = 2;
	indices[3] = 0;
	indices[4] = 2;
	indices[5] = 3;

	glEnableVertexAttribArray(vertex_location);
	glVertexAttribPointer(vertex_location, 3, GL_FLOAT, false, 32, geometry);

	glDrawElements(GL_TRIANGLES, 6, GL_UNSIGNED_INT, indices);

	glDisableVertexAttribArray(vertex_location);
}*/
